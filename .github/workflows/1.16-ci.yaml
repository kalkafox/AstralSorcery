name: Astral Sorcery build

on:
    push:
        branches: [1.16-ci]
    workflow_dispatch:
jobs:
    build:
        runs-on: self-hosted

        container:
            image: gradle:6.8.1
        steps:
            - uses: actions/checkout@v2
            - uses: actions/setup-java@v2
              with:
                  java-version: '8'
                  distribution: 'zulu'
            - name: Check Java version
              run: java -version
            - name: Check OS
              run: uname -r
            - name: Build with Gradle
              run: gradle -Dorg.gradle.jvmargs='-Xmx3G' build
            - uses: "marvinpinto/action-automatic-releases@latest"
              with:
                  repo_token: "${{ secrets.GITHUB_TOKEN }}"
                  automatic_release_tag: "1.16-ci"
                  prerelease: false
                  files: build/libs/*.jar
